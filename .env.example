# Optional. The app runs fully without this — plans are generated deterministically.
# Set this only if you wire up the optional AI generation path.
OPENAI_API_KEY=

# Optional port override (defaults to 3000)
PORT=3000
